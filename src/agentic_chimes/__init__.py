"""Agentic ChIMES: a tool-calling CLI and Python API for building ChIMES
machine-learned interatomic potentials end to end, on top of the pristine
vendored forks under ``codes/``.

See the top-level README.md and docs/ for the full workflow. Every stage is
importable directly (``agentic_chimes.stages.<stage>.run``) as well as
reachable via the ``chimes-agent`` console command (``agentic_chimes.cli``).
"""

__version__ = "0.1.0"
